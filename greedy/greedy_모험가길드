n = int(input())
a = list(map(int, input().split())) # 공포도 입력 

count = 0 # 인원 담는 그릇 변수 생성
result = 0 # 그룹 그릇 변수 생성
a.sort()

for i in a:
    count = count + 1
    if count >= i: # 인원이 공포도 보다 크거나 같다면 
        result = result + 1 # 그룹 생성
        count = 0 # 생성되면 초기화
        
print(result) # 그룹 출력 
